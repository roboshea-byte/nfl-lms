{
  "name": "nfl-lms",
  "version": "1.0.0",
  "private": true,
  "description": "NFL Last Man Standing with personal player links and Postgres",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js",
    "dev": "node --env-file-if-exists=.env.local scripts/dev.js"
  },
  "dependencies": { "pg": "^8.16.3" },
  "devDependencies": { "pg-mem": "^3.0.5" }
}
